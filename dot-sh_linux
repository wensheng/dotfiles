alias dir='ls -lrt'
alias df='df -HT'
export PATH="$PATH:/opt/nvim-linux-x86_64/bin"
alias vi='nvim'
alias mm=micromamba
alias cinit='eval "$(/home/wensheng/miniconda3/bin/conda shell.zsh hook)" && conda init'

FNM_PATH="$HOME/.local/share/fnm"                                                                                                                                                  
if [ -d "$FNM_PATH" ]; then                                                                                                                                                        
  export PATH="$HOME/.local/share/fnm:$PATH"                                                                                                                                       
  eval "`fnm env`"                                                                                                                                                                 
fi   
